<%- include('../partials/header') %>

<div class="container mt-5">
  <h1 class="text-center mb-4">Adicionar Prato a <%= restaurante.name %></h1>

  <form action="/restaurants/<%= restaurante._id %>/add-menu" method="POST" enctype="multipart/form-data" class="card p-4 shadow-sm mx-auto" style="max-width: 600px;">
    <div class="mb-3">
      <label for="name" class="form-label">Nome do prato</label>
      <input type="text" class="form-control" id="name" name="name" required>
    </div>

    <div class="mb-3">
      <label for="category" class="form-label">Categoria</label>
      <select class="form-select" id="category" name="category" required>
        <option disabled selected value="">Escolhe uma categoria</option>
        <option value="Carne">Carne</option>
        <option value="Peixe">Peixe</option>
        <option value="Vegetariano">Vegetariano</option>
        <option value="Sobremesa">Sobremesa</option>
      </select>
    </div>

    <div class="mb-3">
      <label for="description" class="form-label">Descrição</label>
      <textarea class="form-control" id="description" name="description" rows="3" required></textarea>
    </div>

    <div class="mb-3">
      <label for="image" class="form-label">Imagem do prato</label>
      <input type="file" class="form-control" id="image" name="image" accept="image/*" required>
    </div>

    <div class="mb-3">
      <label for="nutrition" class="form-label">Informação Nutricional</label>
      <input type="text" class="form-control" id="nutrition" name="nutrition">
    </div>

    <div class="mb-3">
      <label class="form-label">Preços por dose (€)</label>
      <div class="input-group mb-2">
        <span class="input-group-text">Meia Dose</span>
        <input type="number" step="0.01" min="0" name="meia" id="priceMeia" class="form-control" required>
      </div>
      <div class="input-group mb-2">
        <span class="input-group-text">1 Dose</span>
        <input type="number" step="0.01" min="0" name="inteira" id="priceInteira" class="form-control" required>
      </div>
    </div>

    <button type="submit" class="btn btn-success w-100">Adicionar Prato</button>
  </form>

  <div class="text-center mt-3">
    <a href="/restaurants/list" class="text-decoration-none">← Voltar</a>
  </div>
</div>

<script>
  const form = document.querySelector('form');
  form.addEventListener('submit', function (e) {
    const meia = parseFloat(document.getElementById('priceMeia').value);
    const inteira = parseFloat(document.getElementById('priceInteira').value);

    if (meia < 0 || inteira < 0) {
      e.preventDefault();
      alert('Os preços não podem ser negativos.');
      return;
    }

    if (meia > inteira) {
      e.preventDefault();
      alert('A meia dose não pode ser mais cara que a dose inteira.');
    }
  });
</script>

<%- include('../partials/footer') %>
